langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_job_expiry_date
    - node.type.job
  module:
    - datetime
id: node.job.field_job_expiry_date
field_name: field_job_expiry_date
entity_type: node
bundle: job
label: 'Job Expiry Date'
description: 'The date when the job posting will expire. If a job posting never expires, or you do not know when the job will expire, do not include this property.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: datetime
